import {useState} from 'react';import {Upload,Image as ImageIcon,X} from 'lucide-react';export default function UploadPictures(){const[files,setFiles]=useState<File[]>([]);const add=(e:any)=>setFiles([...files,...Array.from(e.target.files as FileList)].slice(0,20));return <main className="page-enter"><section className="bg-ink py-20 text-center text-white"><h1 className="text-5xl font-black">Upload Vehicle Pictures</h1><p className="mt-3 text-slate-300">Front • Rear • Side • Interior • Engine bay • Dashboard • Additional images</p></section><section className="py-16"><div className="mx-auto max-w-5xl px-5"><label className="grid min-h-52 cursor-pointer place-items-center rounded-3xl border-2 border-dashed border-buddy-300 bg-buddy-50 text-center"><div><Upload className="mx-auto text-buddy-600"/><b className="mt-2 block">Choose up to 20 images</b><span className="text-xs text-slate-500">JPG, PNG, WEBP</span></div><input multiple accept="image/*" type="file" className="hidden" onChange={add}/></label><div className="mt-6 grid grid-cols-2 gap-4 md:grid-cols-4">{files.map((f,i)=><div className="relative overflow-hidden rounded-2xl border"><img src={URL.createObjectURL(f)} className="h-40 w-full object-cover"/><button onClick={()=>setFiles(files.filter((_,n)=>n!==i))} className="absolute right-2 top-2 rounded-full bg-black/70 p-1 text-white"><X size={15}/></button></div>)}</div><button onClick={()=>alert('Demo upload complete. Connect cloud storage/backend for production.')} className="btn-blue mt-8 w-full"><ImageIcon/> UPLOAD PICTURES ({files.length}/20)</button></div></section></main>}