import {useState} from 'react';import {MessageCircle,X,Send} from 'lucide-react';
export default function FloatingWhatsApp(){const[o,setO]=useState(false);const[name,setName]=useState('');const[type,setType]=useState('General Enquiry');const[msg,setMsg]=useState('');const send=()=>window.open(`https://wa.me/27824080680?text=${encodeURIComponent(`Hi The Autobuddy\nName: ${name}\nEnquiry: ${type}\nDetails: ${msg}`)}`,'_blank');return <div className="fixed bottom-20 right-4 z-50 md:bottom-6">{o&&<div className="mb-3 w-[340px] max-w-[calc(100vw-2rem)] overflow-hidden rounded-3xl bg-white shadow-2xl border"><div className="flex justify-between bg-green-600 p-4 text-white"><div><b>The Autobuddy Assistant</b><p className="text-xs">Online • WhatsApp enquiry</p></div><button onClick={()=>setO(false)}><X/></button></div><div className="space-y-3 p-4"><p className="text-sm">Hi 👋 I’ll collect your details before opening WhatsApp.</p><input className="field" placeholder="Your name" value={name} onChange={e=>setName(e.target.value)}/><select className="field" value={type} onChange={e=>setType(e.target.value)}>{['General Enquiry','Vehicle Inspection','Vehicle Evaluation','Vehicle Sourcing','Make an Offer','Dealer Support'].map(x=><option>{x}</option>)}</select><textarea className="field" rows={4} placeholder="Tell us what you need" value={msg} onChange={e=>setMsg(e.target.value)}/><button className="w-full btn-blue" disabled={!name||!msg} onClick={send}><Send size={16}/> Continue on WhatsApp</button></div></div>}<button onClick={()=>setO(!o)} className="float-pulse grid h-16 w-16 place-items-center rounded-full bg-green-500 text-white shadow-2xl"><MessageCircle size={30}/></button></div>}
